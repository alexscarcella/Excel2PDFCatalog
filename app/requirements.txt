# Excel2PDFCatalog - Dipendenze Necessarie

# PDF Generation
reportlab>=4.0.0,<5.0.0

# Image Processing
pillow>=10.0.0,<11.0.0

# Excel File Handling
# Versioni pinnate per compatibilita' macOS x86_64 (build PyInstaller):
# numpy 2.x recente puo' mancare di wheel macOS Intel e venire compilato da
# sorgente, generando un bundle che non carica le dipendenze a runtime.
# numpy 1.26.4 + pandas 2.2.3 sono testati e stabili anche come frozen app.
numpy==1.26.4
pandas==2.2.3
openpyxl>=3.1.0,<4.0.0

# Note: tkinter e' incluso in Python standard library
# Non serve installarlo tramite pip
